## The same null code, guarded by `is not null` — compiles and runs.

fn add_one(x: Int?) -> Int:
    if x is not null:
        return x + 1
    return 0

fn main():
    print(add_one(41))
    print(add_one(null))
